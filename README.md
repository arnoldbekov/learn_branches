# Учебный проект по Git

Этот проект создан для отработки базовых навыков работы с Git и GitHub:
создание репозитория, коммиты, работа с ветками и деплой через GitHub Pages.

---

## 📁 Структура проекта

---

## 🖥 Содержание проекта

### **index.html**
Минимальная HTML-страница:

- корректная структура документа (`<!DOCTYPE html>`)
- язык страницы: `ru`
- подключение CSS-стилей
- простая разметка с элементом `<header>`, который стилизуется через CSS

### **style.css**
Базовые стили:

- Обнуление отступов `body`
- Чёрный прямоугольник-header (`height: 100px`, `background-color: #000`)

### **.nojykill**
Файл для демонстрации отслеживания файлов Git и выполнения коммитов.

---

## 📘 Изученные команды Git

В ходе работы над проектом были использованы основные команды:

```bash
git init          # Инициализация репозитория
git add .         # Добавление файлов в индекс
git commit -m ""  # Создание коммита
git branch        # Работа с ветками
git checkout      # Переключение веток
git push          # Отправка изменений на GitHub
